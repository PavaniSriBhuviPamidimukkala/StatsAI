import {skills} from '../lib/mockData'; export default function Gaps(){return <div className="card p-6"><h2 className="text-xl font-bold text-[#10233f]">Skill-Gap Detection</h2><p className="text-sm text-slate-500 mb-4">Current competency compared with the required benchmark for the selected role.</p>{skills.map(s=>{const gap=Math.max(0,s.required-s.current);const cls=gap>=25?'bg-orange-100 text-orange-700':gap>=10?'bg-yellow-100 text-yellow-700':'bg-emerald-100 text-emerald-700';return <div key={s.name} className="py-4 border-t border-slate-100 flex items-center gap-4"><div className="w-28 font-semibold text-sm">{s.name}</div><div className="flex-1"><div className="flex justify-between text-xs text-slate-500 mb-1"><span>Current {s.current}%</span><span>Required {s.required}%</span></div><div className="h-2 bg-slate-100 rounded-full"><div className="h-2 bg-[#0b8f87] rounded-full" style={{width:`${s.current}%`}}/></div></div><span className={`text-xs font-bold px-2 py-1 rounded ${cls}`}>{gap>=25?'HIGH':gap>=10?'MEDIUM':'LOW'}</span></div>})}</div>}
