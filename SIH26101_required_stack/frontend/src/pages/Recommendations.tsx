import {courses} from '../lib/mockData'; import {useNavigate} from 'react-router-dom'; export default function Recommendations(){const nav=useNavigate();return <div className="card p-6"><h2 className="text-xl font-bold text-[#10233f]">Personalized Learning Path</h2><div className="mt-3 p-3 bg-cyan-50 border-l-4 border-cyan-500 rounded text-sm">Ranking combines skill gap, job-role relevance, previous learning and course relevance.</div>{courses.map((c,i)=><div key={c.id} className="py-4 border-b border-slate-100 flex justify-between gap-4"><div><span className="text-xs bg-slate-100 px-2 py-1 rounded-full">#{i+1} recommendation</span><h3 className="font-bold mt-2">{c.title}</h3><p className="text-sm text-slate-500">{c.reason}</p><p className="text-xs text-slate-500 mt-1">{c.skill} • {c.level} • {c.duration} • {c.source}</p></div><div className="text-right"><b className="text-teal-700">{c.match}%</b><br/><button onClick={()=>nav('/courses')} className="mt-2 text-xs bg-[#1e5ba6] text-white px-3 py-2 rounded-lg">Open</button></div></div>)}</div>}
