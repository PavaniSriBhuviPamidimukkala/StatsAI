import Header from '../components/Header'; import { STATS_DATA } from '../data/mockData';
export default function Profile({profile,setProfile}:{profile:any;setProfile:(p:any)=>void}){return <><Header title="Official Profile" subtitle="Role, experience and competency baseline"/><div className="panel formPanel"><div className="profileTop"><div className="avatar">{(profile?.name||'RK').split(' ').map((x:string)=>x[0]).join('').slice(0,2)}</div><div><h2>{profile?.name}</h2><p>{profile?.designation}</p></div></div><div className="formGrid"><label>Name<input value={profile?.name||''} onChange={e=>setProfile({...profile,name:e.target.value})}/></label><label>Employee ID<input value={profile?.id||''} onChange={e=>setProfile({...profile,id:e.target.value})}/></label><label>Designation<select value={profile?.designation||''} onChange={e=>{const p=STATS_DATA.profiles.find((x:any)=>x.designation===e.target.value)||profile;setProfile({...profile,...p})}}>{STATS_DATA.profiles.map((p:any)=><option key={p.designation}>{p.designation}</option>)}</select></label><label>Department<input value={profile?.department||''} onChange={e=>setProfile({...profile,department:e.target.value})}/></label><label>Experience (years)<input type="number" value={profile?.experienceYears||0} onChange={e=>setProfile({...profile,experienceYears:Number(e.target.value)})}/></label><label>Zone<input value={profile?.zone||''} onChange={e=>setProfile({...profile,zone:e.target.value})}/></label></div></div><div className="panel"><h3>Role Competency Benchmarks</h3><div className="benchmarkGrid">{Object.entries((STATS_DATA as any).roleRequirements[profile?.designation]||{}).map(([skill,req]:any)=><div className="benchmark" key={skill}><span>{skill}</span><b>{req}% required</b></div>)}</div></div></>}
