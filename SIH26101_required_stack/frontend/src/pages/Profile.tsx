import {useState} from 'react'; import {profile} from '../lib/mockData';
export default function Profile(){const [saved,setSaved]=useState(false); const [p,setP]=useState(profile); const f=(k:string)=>(e:any)=>setP({...p,[k]:e.target.value}); return <div className="card p-6"><h2 className="text-xl font-bold text-[#10233f]">Official Profile</h2><p className="text-sm text-slate-500 mb-5">Profile attributes drive competency mapping and recommendations.</p><div className="grid md:grid-cols-2 gap-4">{[['name','Name'],['designation','Designation'],['department','Department'],['assignment','Current Assignment'],['education','Educational Qualification'],['experience','Years of Experience'],['training','Previous Training']].map(([k,l])=><label key={k} className="text-sm font-semibold text-slate-600">{l}<input value={(p as any)[k]} onChange={f(k)} className="mt-1 w-full border border-slate-200 rounded-lg px-3 py-2 font-normal"/></label>)}</div><button onClick={()=>setSaved(true)} className="mt-5 px-4 py-2 bg-[#1e5ba6] text-white rounded-lg font-semibold">Save profile</button>{saved&&<span className="ml-3 text-sm text-teal-700">Profile saved</span>}</div>}
