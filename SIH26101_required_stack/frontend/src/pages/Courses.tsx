import {useState} from 'react'; import {courses} from '../lib/mockData'; export default function Courses(){const [q,setQ]=useState('');const [src,setSrc]=useState('All');const list=courses.filter(c=>(!q||`${c.title} ${c.skill}`.toLowerCase().includes(q.toLowerCase()))&&(src==='All'||c.source===src));return <div className="card p-6"><h2 className="text-xl font-bold text-[#10233f]">Course Catalogue</h2><div className="grid md:grid-cols-2 gap-3 mt-4"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search courses or skills" className="border border-slate-200 rounded-lg px-3 py-2"/><select value={src} onChange={e=>setSrc(e.target.value)} className="border border-slate-200 rounded-lg px-3 py-2"><option>All</option><option>iGOT Karmayogi</option><option>NSSTA</option></select></div>{list.map(c=><div key={c.id} className="py-4 border-t border-slate-100 mt-3 flex justify-between"><div><b>{c.title}</b><p className="text-sm text-slate-500">{c.skill} • {c.duration} • {c.source}</p></div><span className="text-xs font-bold text-teal-700">{c.match}% match</span></div>)}<p className="text-xs text-slate-400 mt-5">Demo / Mock Integration — replace with approved iGOT and NSSTA APIs when access is available.</p></div>}
