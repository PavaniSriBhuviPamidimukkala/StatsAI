import {useEffect,useState} from 'react'; import {BrowserRouter,Routes,Route,Navigate} from 'react-router-dom'; import Layout from './components/Layout'; import Dashboard from './pages/Dashboard'; import Profile from './pages/Profile'; import Assessment from './pages/Assessment'; import SkillGap from './pages/SkillGap'; import Learning from './pages/Learning'; import './index.css'; import {STATS_DATA} from './data/mockData';
const defaultProfile=(STATS_DATA as any).profiles[0]
export default function App(){const [profile,setProfile]=useState<any>(()=>JSON.parse(localStorage.getItem('stats_ai_profile')||'null')||defaultProfile); const [report,setReport]=useState<any>(()=>JSON.parse(localStorage.getItem('stats_ai_report')||'null')); useEffect(()=>localStorage.setItem('stats_ai_profile',JSON.stringify(profile)),[profile]); useEffect(()=>{if(report)localStorage.setItem('stats_ai_report',JSON.stringify(report));},[report]); return <BrowserRouter><Layout><Routes><Route path="/" element={<Navigate to="/dashboard" replace/>}/><Route path="/dashboard" element={<Dashboard profile={profile} report={report}/>}/><Route path="/profile" element={<Profile profile={profile} setProfile={setProfile}/>}/><Route path="/assessment" element={<Assessment profile={profile} setProfile={setProfile} setReport={setReport}/>}/><Route path="/skill-gap" element={<SkillGap profile={profile} report={report}/>}/><Route path="/learning" element={<Learning report={report}/>}/><Route path="*" element={<Navigate to="/dashboard" replace/>}/></Routes></Layout></BrowserRouter>}
